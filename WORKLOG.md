# Журнал разработки и работы с ИИ

Записывать реальные действия сразу после этапа. Этот файл — подробный рабочий журнал; в README перенести краткий честный итог. Не придумывать ошибки ИИ ради заполнения раздела.

| Дата | Действие и инструмент | Что проверено в сессии | Ошибка / исправление / вывод |
| --- | --- | --- | --- |
| 2026-09-24 | Codex: разбор `TASK.md` по требованиям и строкам, проверка официальной документации OpenRouter, Next.js и Vercel, подготовка `DESIGN.md`. | Codex сопоставил требования брифа и отметил конфликт фокуса и клавиатуры, необходимость серверного ключа, стриминга, отмены, GitHub PR и отдельного репозитория. Личная проверка кандидатом ещё предстоит. | Код пока не создавался. Ошибок агента в коде на этом этапе нет; дизайн требует проверки живым запросом и после деплоя. |
| 2026-09-25 | Codex: поиск и сравнение навыков дизайна по исходным репозиториям; подготовка `DESIGN_SKILLS.md`. | Проверены назначение и способ применения Impeccable, Anthropic frontend-design, UI UX Pro Max, Vercel Web Interface Guidelines и доступного Product Design. Личная оценка предложенного выбора кандидатом ещё предстоит. | Навыки не устанавливались; для короткого задания рекомендован один основной навык с ограниченным набором действий. |
| 2026-09-25 | По просьбе кандидата Codex создал отдельный Git-репозиторий в папке тестового и `.gitignore`. | До изменения проверено, что `.git` был только в родительском `dev`, а папка тестового не была отслежена им; соседние проекты не изменялись. | Первый коммит сохраняет бриф и проектные документы. Родительский репозиторий не удалялся и не очищался. |
| 2026-09-25 | Codex: составил `EXECUTION_PLAN.md`, отдельные задания D01 и T03, таблицу `docs/PROGRESS.md`. | Сверены все этапы с `TASK.md`: Esc, таймаут, частичный ответ после Stop, GitHub PR с merge, README с кратким ИИ-логом и дальнейшими шагами. Реализация и тестовое окружение ещё не начаты. | Первую версию плана дополнили после повторной сверки брифа: Esc, таймаут и merge PR были недостаточно явно указаны. |
| 2026-09-25 | Codex: финальная сверка проектных документов с `TASK.md` и официальными описаниями SSE OpenRouter и отмены Vercel. | Проверены контракт истории, ограничения запроса, работа без ключа на локальном макете, ответственность за таймаут и обязательность деплоя. Код и живой запрос не проверялись. | Исправлены противоречия между `DESIGN.md` и `EXECUTION_PLAN.md`: судьба частичного ответа, усечение контекста, dev/test mock без ключа, разделение таймеров. Деплой перестал быть обязательным условием сдачи, поскольку бриф требует публичный GitHub-репозиторий. |
| 2026-09-25 | Codex: распределил D01–P09 по отдельным веткам и создал девять локальных веток от общего документационного коммита. | Проверены список веток, общий базовый коммит и чистое рабочее дерево; назначены ответственные роли и проверки перед передачей этапа. GitHub remote ещё не подключён. | Финальный PR запланирован из `codex/p09-release` в `main` после приёмки D01–R08; принятые ветки этапов также будут опубликованы, чтобы процесс был виден в GitHub. |
| 2026-09-25 | Codex: по уточнению кандидата изменил Git-процесс на отдельный PR каждого этапа в `main`. | Проверены локальные ветки и отсутствие remote; `gh auth status` сообщил, что текущий токен GitHub недействителен. Сверены способы merge с документацией GitHub. | GitHub нужно подключить до PR D01. После проверок каждого этапа PR сливается merge commit, следующая ветка обновляется от `main`; старое решение об одном финальном PR остаётся в предыдущей записи как история изменения плана. |
| 2026-09-25 | После повторного входа кандидата в GitHub Codex создал публичный `stupidkubik/ai-chat-test`, подключил `origin` и отправил `main`. | `gh auth status` подтвердил вход; GitHub API показал `isPrivate: false` и default branch `main`; локальная `main` отслеживает `origin/main`. Секреты и код приложения ещё не добавлялись. | Предыдущее препятствие с недействительным токеном снято. PR D01 откроется после создания и проверки дизайн-артефактов. |
| 2026-09-25 | По просьбе кандидата Codex подготовил окружение для уже созданного им ключа OpenRouter. | Добавлены отслеживаемый `.env.example` без значения и локальный `.env.local` с пустым полем `OPENROUTER_API_KEY`; проверена существующая запись `.gitignore` для `.env.local`, права локального файла ограничены владельцем. | Само значение ключа кандидат вставит в локальный файл. Ключ не передавался в чат, Git или логи; живой запрос пока не проверялся. |
| 2026-09-25 | Codex: D01, установил локальный Impeccable из `pbakaus/impeccable`, подготовил `docs/ui/UI_SPEC.md`, самостоятельный HTML/CSS-прототип и снимки 1440×900 и 390×844. | Прототип открыт в Chromium через Playwright. На 390/320/200 CSS px нет горизонтального скролла, форма и кнопки в viewport; кнопки высотой 44 px. При фокусе textarea форма получает контрастное кольцо 3 px. Impeccable `detect` запущен один раз. | Первый вариант уводил форму ниже экрана — ограничена высота рабочей области и прокрутка оставлена истории. По замечаниям детектора увеличены мелкие подписи, убраны декоративная пульсация и сочетание тонкой границы с размытой тенью, палитра сделана прохладнее. Прототип остаётся статическим; поведение Enter/Esc задано контрактом для UI-этапа. |
| 2026-09-25 | По отзыву кандидата Codex провёл отдельный UX-проход по активному диалогу; результаты записал в `docs/ui/UX_REVIEW.md`, обновил прототип и UI-контракт. | Текущий экран до правок просмотрен в Chromium на 390×844; замечены непонятная подпись справа, пустое приглашение над непустой историей и две визуально активные кнопки во время потока. После правок пересняты 1440×900 и 390×844; на 390/320/200 px нет горизонтального скролла, форма видна, кнопка «Стоп» высотой 44 px; Tab из поля ведёт на «Стоп», «Отправить» семантически отключена. Impeccable `detect`: 0 находок. | Правый текст убран; приглашение оставлено только для пустого чата; черновик следующего вопроса разрешён во время потока, но «Отправить» отключена до конца ответа, а «Стоп» выделена. Текст 429 уточнён как лимит запросов. |
| 2026-09-25 | Codex: повторно сверил D01 с каждым пунктом `TASK.md`; создал `docs/ui/BRIEF_CHECK.md` и дополнил прототип. | Найдены три недостающих доказательства дизайна: полный вид пустого чата, видимые примеры таймаута/ошибки после части ответа и соответствие семантического списка спецификации. Добавлены `preview.html?state=empty`, снимок 390×844 и `ol/li`; пустой экран проверен в Chromium, ввод включает кнопку отправки, на 200 px нет горизонтального скролла. Основные снимки пересняты; консоль без ошибок, Impeccable `detect`: 0 находок. | Требования OpenRouter, серверного ключа, реального потока, отмены, истории и обработки ошибок отмечены как запланированные проверки следующих этапов, а не как выполненные функции статического прототипа. |
| 2026-09-25 | Codex: исправил замечание бота в PR #1 о коротком viewport. | В Chromium воспроизведено: при 390×400 оболочка оставалась высотой 480 px, нижний край «Стоп» был за пределом экрана. После изменения проверены 390×320, 390×400, 844×390, 200×400; форма и кнопки внутри viewport, горизонтального переполнения нет. Пустой экран проверен на 390×320; добавлен снимок `short-mobile.png`. | Убрана минимальная высота 480 px; при высоте до 480 px уплотнены шапка и отступы. Дополнительно исправлено наложение заголовка пустого состояния на шапку: приглашение прокручивается внутри своей области. Проверка на физическом телефоне остаётся за U04/Q07. |
| 2026-09-25 | Кандидат подтвердил завершение дизайна; Codex проверил, что PR #1 слит, обновил ветку `codex/s02-scaffold` от `origin/main` и создал минимальный Next.js 16 / React 19 / TypeScript каркас. | `npm ci`, `npm run lint`, `npm run typecheck`, `npm run build` прошли; `/` открыт в Chromium, заглушка чата отображается, ошибок консоли после повторной проверки нет. `.env.local` остаётся вне Git. | При первом открытии браузер сообщил 404 для favicon — добавлен `app/icon.svg`. ESLint сначала анализировал локальные файлы инструмента дизайна и выдал 94 предупреждения; эти не относящиеся к приложению файлы исключены из конфигурации, после чего lint прошёл без предупреждений. |
| 2026-09-25 | Codex: проверил и слил PR #2, обновил ветку T03 от `main`, создал локальный mock OpenRouter и тесты на встроенном `node:test`. | Тесты подтвердили SSE с тремя фрагментами и `[DONE]`, 429, разрыв до ответа и посреди SSE, задержки и закрытие соединения после отмены. `npm test` запущен без `OPENROUTER_API_KEY`: 4 проверки прошли. `npm run lint` и `npm run build` прошли; отдельный запуск mock и `curl` вернули 200 и ожидаемые SSE-события. | Первая попытка теста не смогла открыть порт из-за ограничений sandbox (`listen EPERM`); повторный запуск с разрешённым loopback прошёл. Макет слушает только локальный интерфейс и не использует внешний ключ. |
| 2026-09-25 | Codex: начал U04 на ветке `codex/u04-ui` от merge-коммита T03 `fc51b19`; перенёс утверждённый интерфейс в React-компонент и CSS. | `npm run lint`, `npm run typecheck`, `npm run build`, `git diff --check` прошли. В браузере проверены макеты 1440×900 и 390×844, короткий экран 390×320, пустой чат, сообщение, поток, остановка, ошибка с сохранённым фрагментом и dev-переключатели. Tab идёт в поле, затем в «Стоп»; отключённая отправка пропускается; активная отправка доступна с клавиатуры. Production-запуск игнорирует `?demo=1` и `?state=streaming`; консоль ошибок не показала. | При первой проверке прокручиваемая история получала лишний Tab stop, а на коротком экране открывалась с начала и скрывала текущий ответ; добавлен `tabIndex=-1` и прокрутка к последнему сообщению. Stop возвращает фокус в поле. Примеры состояний ограничены dev-средой; сетевого запроса нет. Снимки PNG ещё нужно сохранить в `docs/ui/` для передачи этапа. |
| 2026-09-25 | Codex: провёл код-ревью и визуальную QA U04 перед передачей на проверку; создал [PR #4](https://github.com/stupidkubik/ai-chat-test/pull/4). | Повторно прошли `npm run lint`, `npm run typecheck`, `npm run build`; build подтвердил статический prerender `/`. Desktop 1440×900 и mobile 390×844 захвачены Playwright CLI и сверены с D01; dev-консоль: 0 ошибок и предупреждений. Тесты в этом проходе не запускались. | Убран дублирующий список состояний и небезопасное приведение query к типу; типы и demo-состояния вынесены в `app/chat-types.ts`, фикстуры стали явными. Чтение query ограничено dev-веткой: предыдущая сборка ошибочно переводила production `/` в динамический режим. Добавлены два поясняющих комментария о скролле примеров и локальной отправке; снят несемантический `aria-label` с логотипа. Первая мобильная пара снимков отличалась из-за изменения viewport после загрузки; страница перезагружена на целевом размере, повторное сравнение совпало. QA-отчёт: `design-qa.md`, итог `passed`. |
| 2026-09-25 | Codex: провёл код-ревью и визуальную QA U04 перед передачей на проверку; создал [PR #4](https://github.com/stupidkubik/ai-chat-test/pull/4). | Повторно прошли `npm run lint`, `npm run typecheck`, `npm run build`; build подтвердил статический prerender `/`. Desktop 1440×900 и mobile 390×844 захвачены Playwright CLI и сверены с D01; dev-консоль: 0 ошибок и предупреждений. Тесты в этом проходе не запускались. | Убран дублирующий список состояний и небезопасное приведение query к типу; типы и demo-состояния вынесены в `app/chat-types.ts`, фикстуры стали явными. Чтение query ограничено dev-веткой: предыдущая сборка ошибочно переводила production `/` в динамический режим. Добавлены два поясняющих комментария о скролле примеров и локальной отправке; снят несемантический `aria-label` с логотипа. Первая мобильная пара снимков отличалась из-за изменения viewport после загрузки; страница перезагружена на целевом размере, повторное сравнение совпало. QA-отчёт: `design-qa.md`, итог `passed`. |
| 2026-09-25 | Codex: обработал P2-комментарий бота в [PR #4](https://github.com/stupidkubik/ai-chat-test/pull/4): «Keep the scrollable transcript keyboard-focusable». | Playwright при 390×320: первый Tab фокусирует историю; Page Up уменьшает `scrollTop` с 477 до 406, Page Down возвращает на 477; дальше Tab переводит в textarea и на «Стоп». `npm run lint`, `npm run typecheck`, `npm run build` прошли; `/` остаётся статическим. | История переведена с `tabIndex={-1}` на `tabIndex={0}` и получила явное кольцо `:focus-visible`. Так клавиатурный пользователь может попасть в прокручиваемую область и читать более ранние сообщения. |

| 2026-09-25 | Codex: реализовал A05 в `codex/a05-api` поверх merge-коммита U04 `2005eef`; добавил серверный `/api/chat`, валидацию, потоковую прокладку OpenRouter, таймауты, отмену, mock-конфигурацию, тесты и инструкции; открыл [PR #5](https://github.com/stupidkubik/ai-chat-test/pull/5). | `npm run test` — 16/16; `npm run lint`, `npm run typecheck`, `npm run build`, `git diff --check` — успешно. Реальный локальный POST с пустым JSON вернул 400. Live-вызов через `/api/chat`: HTTP 200, получен текстовый SSE и `[DONE]`. Сканированы 12 файлов `.next/static`; ключ не обнаружен. | Во время ревью найдено затенение функции `providerError` локальной переменной в маппере SSE-ошибки; исправлено до тестов. Live-вызов внутри песочницы вернул 502, повтор с разрешённым сетевым доступом прошёл; ключ и текст ответа не выводились. Отмену на deployed Vercel оставить для P09. Выбрана официальная модель-роутер `openrouter/free`; исполнитель меняется между запросами. |

Для следующих этапов указывать конкретный запрос агенту, изменённые файлы, результат `lint`/`build`/ручного теста, найденный недостаток и своё исправление. Если агент предложил сомнительное решение, записать, почему его отклонили.
